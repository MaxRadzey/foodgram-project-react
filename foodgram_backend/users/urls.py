from django.urls import path, include

# from users.views import TokenView

urlpatterns = [
    # path('auth', include('djoser.urls.authtoken')),
    # path('token/logout/', TokenView.as_view(), name='token'),
]
